{
    UI::OpenDialog(
		   `VBox(
			 `PushButton ("&Add"),
			 `PushButton ("&Edit"),
			 `PushButton ("&Resize"),
			 `PushButton ("&Remove"),
		   `HBox(
			 `PushButton ("&Add"),
			 `PushButton ("&Edit"),
			 `PushButton ("&Resize"),
			 `PushButton ("&Remove")
			 )
			 )
		   );

    UI::UserInput();
    UI::CloseDialog();
}

