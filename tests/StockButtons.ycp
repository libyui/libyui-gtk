{
    UI::OpenDialog(
		   `HBox(
			 `PushButton ("&Add"),
			 `PushButton ("&Edit"),
			 `PushButton ("&Resize"),
			 `PushButton ("&Remove")
			 )
		   );

    UI::UserInput();
    UI::CloseDialog();
}

