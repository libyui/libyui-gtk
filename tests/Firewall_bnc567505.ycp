// bug 567505
// GTK UI crashes when cell not set

{
    UI::OpenDialog(
           `VBox(
               `Table(
                  `header("Name", "Price"),
                  [
                   `item(`id(1), "Chili",        6),
                   `item(`id(2), "Salami Baguette",    nil),
                   `item(nil),  // crash
                   `item(`id(4), "Steak Sandwich",    12)
                   ]
               ),
               `PushButton("&OK")
           )
    );
    UI::UserInput();
    UI::CloseDialog();
}
